<Window x:Class="coursWPF.SuiteControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:coursWPF"
        mc:Ignorable="d"
        Title="SuiteControlWindow" Height="450" Width="800">
    <Grid>
        <Button Content="Ouvrir" HorizontalAlignment="Left" Margin="25,24,0,0" VerticalAlignment="Top" Height="26" Width="122" Click="Button_Click"/>
        <DataGrid AutoGenerateColumns="False" Margin="0,55,10,245" x:Name="data">
            <DataGrid.Columns>
                <DataGridTextColumn Width="150" Header="Nom" Binding="{Binding Nom}" />
                <DataGridTextColumn Width="150" Header="Prénom" Binding="{Binding Prenom}" />
                <DataGridTextColumn Width="150" Header="Telephone" Binding="{Binding Telephone}" />
            </DataGrid.Columns>
        </DataGrid>
        <Image Source="https://www.google.fr/images/branding/googlelogo/2x/googlelogo_color_160x56dp.png" HorizontalAlignment="Left" Height="100" Margin="294,257,0,0" VerticalAlignment="Top" Width="100"/>
        <ProgressBar Value="10" Cursor="Arrow" HorizontalAlignment="Left" Height="52" Margin="43,345,0,0" VerticalAlignment="Top" Width="696"/>


    </Grid>
</Window>
